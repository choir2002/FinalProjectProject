<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pemesanantravel_nusamulya">
    <uses-permission android:name="android.permission.internet" />

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PemesananTravel_NusaMulya">
        <activity
            android:name=".daftarUser.DataCustomer"
            android:exported="false" />
        <activity
            android:name=".pesanTravel.RiwayatPesanAdminActivity"
            android:exported="false" />
        <activity
            android:name=".pesanTravel.PesanUserActivity"
            android:exported="false" />
        <activity
            android:name=".LoginAdminActivity"
            android:exported="false" />

        <activity
            android:name=".MenuAdminActivity"
            android:exported="true" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyD22w2SYfBPcluKBIp6R03ZN0KgGjt4p5w" />
        <activity
            android:name=".MapsActivity"
            android:exported="false"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".LoginUserActivity"
            android:exported="true" />
        <activity
            android:name=".daftarUser.RegisterUserActivity"
            android:exported="true" />
        <activity
            android:name=".pesanTravel.profil"
            android:exported="true" />
        <activity
            android:name=".MenuUserActivity"
            android:exported="true" />
        <activity
            android:name=".Kontak"
            android:exported="true" />
        <activity
            android:name=".VisidanMisi"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>